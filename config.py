# this should be a list of function names as strings
tests_to_run = [
        
        ]


# this should be a nested list of tuples of the parameters.
# functions are indexed by tests_to_run
test_params = [
        ()
        ]


# this is a dictionary of global parameters
global_param = {
        
        }
